version: 2

seeds:
  - name: event_codes_lookup
    description: >
      List of Unique Event Codes i.e. CAMEO and their description as per GDELT documentation
      The mapping includes 317 events coded, and not expected to be changed. 
      
    config:
      delimiter: ";"
      column_types:
        EventCode: string
        EventDescription: string
